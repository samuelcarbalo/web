# SPA fallback — Netlify / Cloudflare Pages / Render static
# Archivos reales (p. ej. google-site-verification) se sirven primero.
# Excluir verificación de Google del rewrite a index.html.
/google*.html  200
/*             /index.html   200
